# Display Pattern Generator RTL

This project implements a Verilog RTL-based display pattern generator that creates an FHD gray-scale test pattern without using frame memory. The design generates VSync, DE, and 8-bit pixel data using horizontal and vertical counters.
